{
  "name": "computer-lab",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "description": "Computer organisation on a discrete-event engine: the adder and its carry, the register file, the single-cycle datapath with every wire lit, control as a state machine, the five-stage pipeline and its hazards, and the memory hierarchy from a trace.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@ee-labs/events": "*",
    "@ee-labs/ui": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@ee-labs/prose": "*"
  }
}
