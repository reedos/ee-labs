{
  "name": "photonics-lab",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "description": "Light on a wire: the photon and its energy, the photodiode as a reverse-biased junction solved by the circuit engine, the fibre's attenuation and dispersion, the link budget, and the Fabry-Perot cavity that has no transfer function.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "pins": "node scripts/pins.mjs",
    "verify": "node scripts/verify.mjs"
  },
  "dependencies": {
    "@ee-labs/photonics": "*",
    "@ee-labs/ui": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@ee-labs/prose": "*"
  }
}
