import React from 'react'

/**
 * The lower panes groups K and L draw: the mode chart with the field across the
 * guide, and the radiation pattern in polar form with its beamwidth marked.
 *
 * Written by the guide and antenna lane.
 */

export function GuidePane() {
  return <p className="hint">The guide view is not built yet.</p>
}

export function PatternPane() {
  return <p className="hint">The pattern view is not built yet.</p>
}
