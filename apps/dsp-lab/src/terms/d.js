// The terms Group D, estimating a spectrum needs.
//
// Empty until that group lands. ../terms.js merges this file, so the group's
// lane adds its definitions here and edits no file another lane owns.

export const TERMS = {}

/** The words that mean a reader has met one of these terms. */
export const TERM_WORDS = {}
