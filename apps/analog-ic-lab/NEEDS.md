# Analog IC Lab: needs

The lab is dark. It is built and served at its own URL and linked from nowhere
until Reed changes RELEASE_STATUS.

The deploy workflow carries this line, added by the director at integration:

```
cp -r apps/analog-ic-lab/dist _site/analog-ic-lab
```
