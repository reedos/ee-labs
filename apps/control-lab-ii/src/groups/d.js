// Group D: the describing function.
//
// The lane that owns this file writes the five experiments of the plan's
// section 5. The group name and the empty list are the seam, landed with the
// shell so `experiments.js` can import it before the lessons exist.

export const GROUP_D = 'The describing function'

export default []
