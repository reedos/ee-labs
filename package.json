{
  "name": "ee-labs",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "description": "Interactive tools for the signals half of an electrical engineering curriculum.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspace apps/signal-lab",
    "lint:prose": "node packages/prose/bin/lint.mjs",
    "site": "node scripts/assemble-site.mjs",
    "site:serve": "node scripts/assemble-site.mjs --serve"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "playwright": "^1.62.1",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
